# Plan: Full Codebase Optimization — Dead Code, Performance, SEO, Accessibility

Deep audit completed. Three areas: cleanup, performance, SEO/GEO/LLM.

---

## 1. Dead Code Removal (12 files)

Delete these files — none are imported anywhere:

| File | Reason |
|------|--------|
| `components/Navbar.tsx` | Empty shell, nav moved to MorphingMenu |
| `components/MobileBottomNav.tsx` | Removed from layout.tsx |
| `components/StickyServices.tsx` | Zero importers |
| `components/ServicesFlipSection.tsx` | Zero importers |
| `components/ServicesSlider.tsx` | Zero importers |
| `components/ServicesSlider.module.css` | Only used by dead ServicesSlider |
| `components/TrustedBy.tsx` | Zero importers |
| `TrustedBy_original.tsx` (root) | Backup file, not referenced |
| `lib/scrollToSection.ts` | Only caller was dead MobileBottomNav |
| `lib/types.ts` | Zero imports — data files define types inline |

Also remove from `package.json`:
- `@base-ui/react` — zero imports found anywhere

---

## 2. Performance Fixes

### 2a. Remove `motion` package (~40KB)
- `components/ui/glowing-effect.tsx` uses `import { animate } from "motion/react"` for a single call
- Replace with `gsap.to()` (already loaded) — eliminates entire dependency

### 2b. Page transitions — add `prefers-reduced-motion` check
- `app/template.tsx` — SVG morph animation runs on every route change without checking reduced motion
- Add check: skip animation if `prefers-reduced-motion: reduce`

### 2c. Components missing `prefers-reduced-motion` checks
- `components/CustomCursor.tsx` — disable cursor entirely for reduced-motion users
- `components/Magnetic.tsx` — skip magnetic effect
- `components/BackgroundBeams.tsx` — render static fallback
- `components/SmoothScroll.tsx` — disable Lenis smooth scroll for reduced-motion users

### 2d. Font optimization
- `layout.tsx` line 19: Outfit loads 7 weights (300-900). Reduce to only used weights (likely 400, 600, 800)
- `layout.tsx` line 33: Cormorant Garamond loads 5 weights × 2 styles = 10 files. Audit actual usage
- `CircularGallery.tsx`: Loads Figtree via runtime `fetch()` bypassing `next/font`. Acceptable since it's vendored

### 2e. Image optimization
- `components/MorphingMenu.tsx`: Logo images use `width={400} height={400}` without `sizes` prop. Add `sizes="192px"`
- `components/FeaturedArchitectures.tsx`: Add `priority` prop to first visible card image

### 2f. Console cleanup
- `components/GrainientBackground.tsx` lines 158, 175: `console.warn` — gate behind `process.env.NODE_ENV === 'development'`
- `components/CircularGallery.tsx` line 108: `console.error` — same treatment

---

## 3. SEO, GEO, and LLM Optimization

### 3a. Add metadata to all pages (currently missing)
These pages are `"use client"` and export no metadata. Fix by extracting metadata to a parent server component or adding separate `layout.tsx` files:

- `app/about/page.tsx` → Add metadata: title "About Reverbex Technologies", description about company
- `app/contact/page.tsx` → Add metadata: title "Contact Us", description with phone/email
- `app/services/page.tsx` → Add metadata: title "Our Services", description listing capabilities
- `app/industries/page.tsx` → Add metadata: title "Industries We Serve", description
- `app/pricing/page.tsx` → Add metadata: title "Pricing", description about transparent pricing

**Approach:** Create `metadata` exports in a wrapper or use `generateMetadata` in a server component parent.

### 3b. Schema.org improvements
- Add `BreadcrumbList` schema to all inner pages (currently only on services/[slug] and work/[slug])
- Add `WebPage` schema to about, pricing, contact, industries pages
- Verify `FAQPage` schema matches actual FAQ content on homepage

### 3c. Heading hierarchy audit
- Ensure every page has exactly one `<h1>`
- Verify h2→h3→h4 nesting is correct on all pages

### 3d. LLM/AEO text blocks
- Add explicit answer blocks in homepage sections that answer common queries:
  - "What does Reverbex do?" — answered in FAQ but also reinforce in hero/about
  - "How much does a website cost?" — add in pricing section
  - "Why choose custom over templates?" — reinforce in WhyReverbex
- Ensure case study pages have clear problem→research→solution→results narrative (already good)

---

## 4. Contact Form UX Fix (keep backend as-is for now)

- `components/ContactSection.tsx` line 291: Empty `catch {}` swallows errors silently
- Add loading state (disable button, show spinner during fetch)
- Show error message on failure instead of false success
- `app/api/contact/route.ts` line 15: Remove `console.log`
- Do NOT wire up email — backend stays as console.log for now

---

## Execution Order

1. Delete dead files (12 files)
2. Remove `@base-ui/react` from package.json
3. Replace `motion` import in glowing-effect.tsx with GSAP
4. Add `prefers-reduced-motion` checks (template, cursor, magnetic, background beams, smooth scroll)
5. Add metadata to all 5 pages missing it
6. Add breadcrumb schemas to inner pages
7. Fix font weights in layout.tsx
8. Fix image sizes in MorphingMenu
9. Add loading/error states to contact form
10. Clean up console.warn/error

---

## Verification

- `npm run build` — zero errors
- `npm run dev` — check all pages load
- Check reduced-motion: toggle in browser DevTools, verify animations stop
- Check Lighthouse scores (Performance, SEO, Accessibility)
- Verify contact form submits correctly
- Check page source for metadata tags on each route
